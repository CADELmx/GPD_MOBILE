import { Link } from "expo-router";
import { Text, View } from "react-native";

export default function createPartialView(){
  return(
    <View>
      <Text> AQUI VA A IR UN FORMULARIO PARA REGISTRAR UNA NUEVA PLANTILLA PARCIAL</Text>
    </View>
      
  )
}