import { ThemedView } from '@/components/ThemedView';
import { ThemedText } from '@/components/ThemedText';

export default function HomeSubjectView() {
  return (
    <ThemedView>
      <ThemedText> AQUÍ SE VISUALIZARÁN LAS MATERIAS </ThemedText>
    </ThemedView>
  )
}