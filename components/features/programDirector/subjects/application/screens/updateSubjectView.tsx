import { ThemedView } from '@/components/ThemedView';
import { ThemedText } from '@/components/ThemedText';

export default function UpdateSubjectView() {
  return (
    <ThemedView>
      <ThemedText> AQUÍ SE ACTUALIZARÁN LAS MATERIAS </ThemedText>
    </ThemedView>
  )
}