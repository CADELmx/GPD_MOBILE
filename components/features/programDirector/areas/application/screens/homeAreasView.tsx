import { ThemedView } from "@/components/ThemedView";
import { ThemedText } from '@/components/ThemedText';

export default function HomeAreasView() {
  return (
    <ThemedView>
      <ThemedText> AQUÍ SERÁ LA VISTA PRINCIPAL DE LAS AREAS </ThemedText>
    </ThemedView>
  )
}